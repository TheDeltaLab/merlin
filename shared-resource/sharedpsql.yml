name: shared
type: AzurePostgreSQLFlexible
project: brainly
# test ring reuses the legacy delta-shared-test-krc-pg (see docs/legacy-resources.md); not created here.
ring:
  - staging
region:
  - koreacentral
  - eastasia

dependencies: []

defaultConfig:
  location: ${ this.region }
  skuName: Standard_D2ds_v4
  tier: GeneralPurpose
  storageSizeGb: 32
  version: "16"

  tags:
    merlin: "true"
    env: ${ this.ring }

specificConfig: []

exports: {}
