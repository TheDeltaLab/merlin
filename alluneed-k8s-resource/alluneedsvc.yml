name: alluneed
type: KubernetesService
project: alluneed
ring:
  - test
  - staging
region:
  - koreacentral
  - eastasia

dependencies:
  - resource: KubernetesDeployment.alluneed
    isHardDependency: true

defaultConfig:
  namespace: alluneed
  serviceType: ClusterIP
  appName: alluneed
  ports:
    - port: 8000
      targetPort: 8000

specificConfig: []

exports: {}
