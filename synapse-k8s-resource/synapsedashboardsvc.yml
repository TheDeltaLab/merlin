name: synapse-dashboard
type: KubernetesService
project: synapse
ring:
  - test
  - staging
region:
  - koreacentral

dependencies:
  - resource: KubernetesDeployment.synapse-dashboard
    isHardDependency: true

defaultConfig:
  namespace: synapse
  serviceType: ClusterIP
  appName: synapse-dashboard
  ports:
    - port: 3001
      targetPort: 3001

specificConfig: []

exports: {}
