name: trinity-lance-worker
type: KubernetesService
project: merlin
ring:
  - test
  - staging
region:
  - koreacentral
  - eastasia

dependencies:
  - resource: KubernetesDeployment.trinity-lance-worker
    isHardDependency: true

defaultConfig:
  namespace: trinity
  serviceType: ClusterIP
  appName: trinity-lance-worker
  ports:
    - port: 3000
      targetPort: 3000

specificConfig: []

exports: {}
